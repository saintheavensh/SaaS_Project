//apps/backend/src/modules/users/controller.ts
import { Context } from 'hono';

// Placeholders for Step 1
export const createUser = async (c: Context) => { return c.json({}); };
export const updateUser = async (c: Context) => { return c.json({}); };
export const getUsers = async (c: Context) => { return c.json({}); };
export const getUserById = async (c: Context) => { return c.json({}); };
export const getProfile = async (c: Context) => { return c.json({}); };
